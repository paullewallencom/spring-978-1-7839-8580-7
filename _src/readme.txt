To run the code for a recipe:

- at the root of the recipe folder (where the pom.xml file is):
mvn clean install

- A "springwebapp.war" file has been created in the "target" folder. Execute it using a Java web server (ex: Tomcat).

For more details, see Chapter 1 in the book.
